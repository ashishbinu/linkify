{
  "name": "linkify",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch src/ --exec 'clear && pnpm run build'",
    "build": "esbuild ./src/content-script.js --bundle --outfile=./build/content-script.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autolinker": "^4.0.0",
    "esbuild": "^0.17.18"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
